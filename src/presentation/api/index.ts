/**
 * API Presentation Layer — Express routes and server.
 *
 * Exports the app factory for testing and the server startup for production.
 */

export { createApp, startServer } from './server.js';
export { createReturnsRouter } from './returnsRoutes.js';
